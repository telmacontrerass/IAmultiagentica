"""CLI subcommand handlers for ci2lab.

Each module implements one ``ci2lab`` subcommand (agent, chat, doctor,
hardware, models, sessions, skills, evals, ui) and is wired up in
``ci2lab.cli.main``.
"""

CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.

- Do NOT use read_file, bash, grep, glob, write_file, edit_file, or ANY other tool.
- You already have all the context you need in the transcript below.
- Your entire response must be plain text.

You are summarizing an agent coding session so it can continue with less context. Capture everything essential for continuing the work without losing information.

Your summary must include these sections:

1. User requests: every explicit request and intent from the user, in order.
2. Files and changes: each file that was read, created, or edited, with the key code or content involved. Include exact file names and paths.
3. Errors and fixes: errors hit and how they were resolved (or not). Include user corrections — if the user said to do something differently, record it.
4. Current state: what was being worked on most recently and what is done vs pending.
5. Next step: the immediate next action, only if it directly follows from the user's most recent request.

Rules:

- Be specific: exact file names, function names, commands, and short code snippets matter more than prose.
- Do not invent anything that is not in the transcript.
- Keep it compact: aim for less than 500 words.
